{
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "parcel watch main.js --out-dir prod/",
    "build": "parcel build main.js --out-dir prod/"
  },
  "dependencies": {
    "gsap": "^3.12.2",
    "three": "^0.134.0"
  }
}
